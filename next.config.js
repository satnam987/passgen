/** @type {import('next').NextConfig} */

// Eenvoudige Next.js configuratie zonder Prisma
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Any experimental features can go here
  },
};

module.exports = nextConfig; 